server:
  port: 5000
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://testdb.civ1pxw9zovk.eu-central-1.rds.amazonaws.com/testdb"
    username: postgres
    password: postgres
    hikari:
      maximum-pool-size: 10
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false